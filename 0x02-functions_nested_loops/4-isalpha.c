#include "main.h"


/**
 * _isalpha - checks for alphabets
 * @c: character to be checked
 *
 * Return: 1 if character is a letter,0 otherwise
 */
int _isapha(inc c)
{
	if ((c >= 'a' && c <= 'z') || (c >= 'A' && C <= 'z'))

			return (1);
		else
			return (0);
}
